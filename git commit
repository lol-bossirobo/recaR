Steps

cd ---------------->home directory ~

cd d: cd Desktop mkdir Prad

cd Prad

git clone (github code link paste)

ls---->list all files

git add (filename)

git remote add (user) <git link>

git status----->check all files are added

git commit -m "message" //"message "---->any message git remote add (any name) (github code link paste)

git push -u (user from line 15) //u user